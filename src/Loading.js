import React from 'react'

const Loading = (props) => <h2>{props.message}</h2>

export default Loading;
//if multiple put export befor const and delete line 5
//more than 1 consst: put loading in crlybrackets in app.js
